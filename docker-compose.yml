services:
  neox-consensus:
    build: .
    container_name: neox-consensus
    restart: unless-stopped
    volumes:
      - ./node-data:/data
    ports:
      - "30303:30303"
      - "30303:30303/udp"
      - "8545:8545"
      - "8546:8546"
    environment:
      - EXTERNAL_IP=0.0.0.0
